{
  "id": "ecienciaTelegramMenuReservas",
  "name": "Eciencia - Telegram menu del dia y reservas",
  "active": false,
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "eciencia-enviar-menu-manual",
        "responseMode": "onReceived",
        "responseData": "firstEntryJson",
        "options": {
          "responseCode": 202
        }
      },
      "id": "ea3529c4-91f1-4a3d-a3a8-69e0bd4deaf8",
      "name": "Webhook - enviar menu manual",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2.1,
      "position": [
        -900,
        -420
      ],
      "webhookId": "eciencia-enviar-menu-manual"
    },
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 9 * * 1-5"
            }
          ]
        }
      },
      "id": "d4c1624d-76cc-4d96-8d6c-2d9f47a7b101",
      "name": "Enviar menu 09:00",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        -900,
        -220
      ]
    },
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "30 2 * * *"
            }
          ]
        }
      },
      "id": "96d20c5d-51ea-4c77-9726-1f77fe24cc37",
      "name": "Limpiar imagenes 02:30",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        -900,
        -20
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "function cfg(name, fallback = '') {\n  const vars = typeof $vars === 'undefined' ? {} : $vars;\n  const env = typeof $env === 'undefined' ? (typeof process === 'undefined' ? {} : process.env) : $env;\n  return env[name] || vars[name] || fallback;\n}\n\nconst backendUrl = (cfg('N8N_ECIENCIA_BACKEND_URL') || cfg('PUBLIC_BACKEND_URL')).replace(/\\/$/, '');\nconst cleanupSecret = cfg('N8N_MENU_WEBHOOK_SECRET');\n\nif (!backendUrl || !cleanupSecret) {\n  throw new Error('Faltan N8N_ECIENCIA_BACKEND_URL y N8N_MENU_WEBHOOK_SECRET.');\n}\n\nconst result = await helpers.httpRequest({\n  method: 'POST',\n  url: backendUrl + '/api/menu/system/limpiar-imagenes',\n  headers: {\n    'X-Eciencia-Webhook-Secret': cleanupSecret,\n  },\n  body: {},\n  json: true,\n});\n\nreturn [\n  {\n    json: {\n      ...result,\n      executedAt: new Date().toISOString(),\n    },\n  },\n];"
      },
      "id": "b3704e66-9a5a-4e33-b922-b8668a592a25",
      "name": "Limpiar imagenes antiguas",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -640,
        -20
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const fs = require('fs');\nconst code = fs.readFileSync('/scripts/preparar-foto-y-sesiones.js', 'utf8');\nreturn eval(code);"
      },
      "id": "3799ce6e-4a74-456e-b36d-f5210e98a8bc",
      "name": "Preparar foto y sesiones",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -640,
        -220
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "function cfg(name, fallback = '') {\n  const vars = typeof $vars === 'undefined' ? {} : $vars;\n  const env = typeof $env === 'undefined' ? (typeof process === 'undefined' ? {} : process.env) : $env;\n  return env[name] || vars[name] || fallback;\n}\n\nconst TELEGRAM_BOT_TOKEN = cfg('TELEGRAM_BOT_TOKEN');\nif (!TELEGRAM_BOT_TOKEN) {\n  throw new Error('Falta TELEGRAM_BOT_TOKEN en n8n.');\n}\n\nconst SUPABASE_URL = cfg('SUPABASE_URL').replace(/\\/$/, '');\nconst SUPABASE_KEY = cfg('SUPABASE_SERVICE_ROLE_KEY') || cfg('SUPABASE_KEY');\n\nfunction cleanInlineReplyMarkup(markup) {\n  const keyboard = markup?.inline_keyboard || [];\n  const rows = keyboard\n    .map((row) => (Array.isArray(row) ? row : []))\n    .map((row) => row.filter((button) => button?.text && button?.callback_data))\n    .filter((row) => row.length);\n  return rows.length ? { inline_keyboard: rows } : null;\n}\n\nasync function markMenuSent(subscriptionId) {\n  if (!subscriptionId || !SUPABASE_URL || !SUPABASE_KEY) return;\n\n  await helpers.httpRequest({\n    method: 'PATCH',\n    url: SUPABASE_URL + '/rest/v1/telegram_subscriptions?id=eq.' + encodeURIComponent(subscriptionId),\n    headers: {\n      apikey: SUPABASE_KEY,\n      Authorization: 'Bearer ' + SUPABASE_KEY,\n      'Content-Type': 'application/json',\n      Prefer: 'return=minimal',\n    },\n    body: { last_menu_sent_at: new Date().toISOString() },\n    json: true,\n  });\n}\n\nconst output = [];\nfor (const item of items) {\n  const data = item.json || {};\n  const body = {\n    chat_id: data.chatId,\n    photo: data.photoUrl,\n    caption: data.caption || '',\n    parse_mode: 'HTML',\n  };\n  const replyMarkup = cleanInlineReplyMarkup(data.inlineKeyboard);\n  if (replyMarkup) body.reply_markup = replyMarkup;\n\n  const response = await helpers.httpRequest({\n    method: 'POST',\n    url: 'https://api.telegram.org/bot' + TELEGRAM_BOT_TOKEN + '/sendPhoto',\n    body,\n    json: true,\n  });\n\n  if (response.ok === true) await markMenuSent(data.subscriptionId);\n\n  output.push({\n    json: {\n      ...data,\n      telegramOk: response.ok === true,\n      telegramMessageId: response.result?.message_id || null,\n    },\n  });\n}\n\nreturn output;"
      },
      "id": "8e2f527a-fb64-45d6-a7b2-6a6a0292588a",
      "name": "Telegram - enviar foto menu",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -360,
        -220
      ]
    },
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "59 23 * * *"
            }
          ]
        }
      },
      "id": "trigger-expirar-menu",
      "name": "Expirar menu activo 23:59",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        -900,
        180
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "function cfg(name, fallback = '') {\n  const vars = typeof $vars === 'undefined' ? {} : $vars;\n  const env = typeof $env === 'undefined' ? (typeof process === 'undefined' ? {} : process.env) : $env;\n  return env[name] || vars[name] || fallback;\n}\n\nconst backendUrl = (cfg('N8N_ECIENCIA_BACKEND_URL') || cfg('PUBLIC_BACKEND_URL')).replace(/\\/$/, '');\nconst cleanupSecret = cfg('N8N_MENU_WEBHOOK_SECRET');\n\nif (!backendUrl || !cleanupSecret) {\n  throw new Error('Faltan N8N_ECIENCIA_BACKEND_URL y N8N_MENU_WEBHOOK_SECRET.');\n}\n\nconst result = await helpers.httpRequest({\n  method: 'POST',\n  url: backendUrl + '/api/menu/system/expirar-activo',\n  headers: {\n    'X-Eciencia-Webhook-Secret': cleanupSecret,\n  },\n  body: {},\n  json: true,\n});\n\nreturn [\n  {\n    json: {\n      ...result,\n      executedAt: new Date().toISOString(),\n    },\n  },\n];"
      },
      "id": "code-expirar-menu",
      "name": "Expirar menu en backend",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -640,
        180
      ]
    }
  ],
  "connections": {
    "Webhook - enviar menu manual": {
      "main": [
        [
          {
            "node": "Preparar foto y sesiones",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Enviar menu 09:00": {
      "main": [
        [
          {
            "node": "Preparar foto y sesiones",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Limpiar imagenes 02:30": {
      "main": [
        [
          {
            "node": "Limpiar imagenes antiguas",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Preparar foto y sesiones": {
      "main": [
        [
          {
            "node": "Telegram - enviar foto menu",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Expirar menu activo 23:59": {
      "main": [
        [
          {
            "node": "Expirar menu en backend",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "timezone": "America/Bogota"
  },
  "versionId": "b89f7ddd-7067-469b-9c05-bfe437543f9c",
  "versionCounter": 53,
  "exportedAt": "2026-06-10T00:00:00.000Z",
  "note": "Export without credentials. Fill environment variables from backend/n8n/eciencia-n8n.env.example before running."
}